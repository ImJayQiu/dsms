<% if can? :manage, :settings %>

	<%= form_for(@settings_experiment) do |f| %>
	<% if @settings_experiment.errors.any? %>
	<div id="error_explanation">
	<h2><%= pluralize(@settings_experiment.errors.count, "error") %> prohibited this settings_experiment from being saved:</h2>

	<ul>
		<% @settings_experiment.errors.full_messages.each do |message| %>
			<li><%= message %></li>
	<% end %>
	</ul>
</div>
  <% end %>

<ul>
	<li>Name: <%= f.text_field :name %></li>
	<p/>
	<li>Full Name: <%= f.text_field :fullname %></li>
	<p/>
	<li>Description: <%= f.text_field :description %></li>
	<p/>
</ul>
<table>
	<tr>
		<td>
			<%= f.submit %>
		</td>
		<% end #form %>
		<td>
			<%= button_to 'Back', settings_experiments_path, method: :get %>
		</td>
	</tr>
</table>

<%else%>
	Access Denied !
<%end #can%>


<% if can? :manage, :settings %>

	<%= form_for(@settings_temporal) do |f| %>
	<% if @settings_temporal.errors.any? %>
	<div id="error_explanation">
	<h2><%= pluralize(@settings_temporal.errors.count, "error") %> prohibited this settings_temporal from being saved:</h2>

	<ul>
		<% @settings_temporal.errors.full_messages.each do |message| %>
			<li><%= message %></li>
	<% end %>
	</ul>
</div>
  <% end %>

<ul>
	<li>Name: <%= f.text_field :name %></li>
	<p/>
	<li>Remark: <%= f.text_field :remark %></li>
	<p/>
</ul>

<table>
	<tr>
		<td>
			<%= f.submit %>
		</td>
		<% end #form %>
		<td>
			<%= button_to 'Back', settings_temporals_path, method: :get %>
		</td>
	</tr>
</table>

<%else%>
	Access Denied !
<%end #can%>

<% if can? :manage, :settings %>

	<%= form_for(@settings_ensemble) do |f| %>
	<% if @settings_ensemble.errors.any? %>
	<div id="error_explanation">
	<h2><%= pluralize(@settings_ensemble.errors.count, "error") %> prohibited this settings_ensemble from being saved:</h2>

	<ul>
		<% @settings_ensemble.errors.full_messages.each do |message| %>
			<li><%= message %></li>
	<% end %>
	</ul>
</div>
  <% end %>

<ul>
	<li>Name: <%= f.text_field :name %></li>
	<p/>
	<li>Full Name: <%= f.text_field :fullname %></li>
	<p/>
	<li>Description: <%= f.text_field :description %></li>
	<p/>
</ul>

<table>
	<tr>
		<td>
			<%= f.submit %>
		</td>
		<% end #form %>
		<td>
			<%= button_to 'Back', settings_ensembles_path, method: :get %>
		</td>
	</tr>
</table>

<% else %>
	Access Denied !
<% end #can %>

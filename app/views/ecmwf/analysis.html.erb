<table>
	<tr>
	</tr>
	<tr>
		<td style="width:50%;" align="center">
			<a href= <%= root_path+"#{@cdo_output_path}_all.gif" %> target = "_blank" >
				<%= image_tag root_path+"#{@cdo_output_path}_all.gif", width: "50%" %>
				<br/>Click image to enlarge
			</a>
		</td>
	</tr>
</table>
<p/>
<table>
	<tbody>
	<tr>
		<% if can? :download, :nc %>
			<td>
			<%= link_to "Download NC", @sys_output_dir.to_s+@output_name.to_s+".nc", download: "#{@date}_#{@type}_#{@var}_#{@lon_lat}.nc" %>
			<%= button_to "Download NC", @sys_output_dir.to_s+@output_name.to_s+".nc", method: :get %>
		</td>
		<% end # if can? :download, :nc %>
		<% if can? :download, :ctl %>
			<td>
			<%= link_to "Download CTL", @sys_output_dir.to_s+@output_name.to_s+".ctl", download: "#{@date}_#{@type}_#{@var}_#{@lon_lat}.ctl" %>
			<%= button_to "Download CTL", @sys_output_dir.to_s+@output_name.to_s+".ctl", method: :get %>
		</td>
		<% end # if can? :download, :ctl %>
		<% if can? :download, :csv %>
			<td>
			<%= link_to "Download CSV", @sys_output_dir.to_s+@output_name.to_s+".csv", download: "#{@date}_#{@type}_#{@var}_#{@lon_lat}.csv" %>
			<%= button_to "Download CSV", @sys_output_dir.to_s+@output_name.to_s+".csv", method: :get %>
				</td>
			<% end # if can? :download, :csv %>
	</tr>
	</tbody>
</table>
